# 更新记录

## V1.4.1

### 新功能

- **多从站架构**：服务器（从站）支持创建多个独立实例，每个从站拥有独立数据区、监听端口与运行状态；可新增、编辑、删除、启停。
- **从站独立点表**：从站不再依赖主站寄存器字典，改用自身独立点表，支持新增、编辑、删除点位及导入/导出 CSV。
- **UDP 协议**：主站客户端与从站服务器均新增 Modbus UDP 支持（帧格式同 TCP/MBAP，无连接）。
- **从站全局生命周期**：从站运行状态提升至全局，切换到其他页面后从站继续运行，事件持续处理。
- **主站访问多从站**：字典项可指定目标从站地址，轮询时按从站分组读取；新建从站对话框按协议切换串口/网络配置。
- **主站分组切换**：字典表格上方新增分组标签页，按分组切换显示不同数据。
- **报文窗口可拖拽/隐藏**：主页报文日志窗口支持拖拽分隔条调整高度，并可一键隐藏/展开。
- **从站实例编辑**：可编辑从站名称、从站地址、协议、串口/监听配置（运行中需先停止）。

### 优化

- **术语友好化**：导航与文案 Client/Server 改为「客户端（主站）/服务器（从站）」，「从机」统一为「从站」。
- **通信统计独立计数**：发送/接收/错误计数改为独立累加，不再受日志上限影响；新增「重置统计」按钮。
- **日志容量提升**：报文日志上限由 1000 条提升至 10000 条。
- **渲染性能**：报文日志改为滚动动态加载，避免数据量大时切换页面卡顿。
- **CSV 编码兼容**：字典导入支持 GBK 解码兜底，修复 WPS/Excel 编辑后导入乱码。

### 问题修复

- 修复 RTU 从站串口路径写死为空导致无法启动的问题。
- 修复主站报文窗口最后一行被裁切看不到的问题。
- 修复循环周期输入框上下调整按钮被裁掉的问题。

## V1.1.4

- 寄存器字典支持自定义排序（上移/下移）。
- 寄存器字典页面新增 Modbus 地址区段说明注释。
- 寄存器字典支持导入/导出（CSV / JSON）。
- 报文日志支持导出（CSV / TXT）。
- 报文日志新增按地址区段筛选（线圈/离散输入/输入寄存器/保持寄存器）。
- Client 与 Server 新增更多波特率选项（1200 ~ 921600）。
- CRC 校验失败时错误消息附带原始 HEX 响应数据，便于排查。

## V1.1.3

- Client 改为寄存器字典驱动读取。
- Server 改为按寄存器字典生成数据区。
- 支持保存 Client 数据、Server 数据、寄存器字典、工程配置和报文日志。
- 修复 Electron 打包后静态资源绝对路径导致白屏的问题。
- 增加开源项目 README、贡献指南、安全策略和 MIT 协议。

## V1.1.0

- 增加 Modbus TCP Client。
- 增加 Modbus TCP Server 和 Modbus RTU Server。
- 增加工程管理、寄存器字典和报文日志视图。
